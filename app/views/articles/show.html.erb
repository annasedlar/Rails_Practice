
<p>
<strong>Title:</strong>
<%= @article.title %>
</p>

<p>
<strong>Text:</strong>
<%= @article.text %>
</p>

<hr>
<%= link_to "Edit", edit_article_path(@article), :class => 'btn btn-warning' %>
<%= link_to "Delete", edit_article_path(@article),
                    method: :delete,
                    data: {confirm: "Are you sure?"},
                    :class => 'btn btn-danger' %>
            
